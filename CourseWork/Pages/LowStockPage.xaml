<Page x:Class="CourseWork.Pages.LowStockPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CourseWork.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="1200"
      Title="Отчёт: Низкая наполненность">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Отчёт: Низкая наполненность автоматов" FontSize="24" FontWeight="Bold"/>
            <Button x:Name="btnRefresh" Content="Обновить" Margin="20,0,0,0" Width="120" Click="btnRefresh_Click"/>
        </StackPanel>

        <ListView x:Name="lvLowStock" Grid.Row="1">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Серийный номер" Width="180" DisplayMemberBinding="{Binding SerialNumber}"/>
                    <GridViewColumn Header="Товар" Width="300" DisplayMemberBinding="{Binding ProductName}"/>
                    <GridViewColumn Header="В наличии" Width="120" DisplayMemberBinding="{Binding Quantity}"/>
                    <GridViewColumn Header="Мин. остаток" Width="120" DisplayMemberBinding="{Binding MinLevel}"/>
                    <GridViewColumn Header="К загрузке (шт.)" Width="150" DisplayMemberBinding="{Binding NeedToLoad}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>